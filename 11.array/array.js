//  const names = ["Ade", "Budi", "Cici", "Deni"];

// names.splice(1, 1);
// names.length
// names.push("Eka");
// names.reverse();
// names[2] = "Eka"; // boleh ganti value jadi = 1, undefined, dll

// console.log(names);


const grid = [
    [1, 2, 3], // index 0
    [4, 5, 6], // index 1
    [7, 8, 9]  // index 2
];

console.log(grid[1][2]);