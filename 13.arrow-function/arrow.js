const jumlah = (x, y) => x + y;

console.log(sum(1, 2));
console.log(jumlah(1, 2));


function sum(x, y) {
        return x + y;
    }
    
    // const jumlah = (x, y) => {
    //     return x + y;
    // };

    // console.log(sum(1, 2));
    // console.log(jumlah(1, 2));